#include <stdio.h>
/**
 *  * main - Entry point
 *   *
 *    * Return: Always 0 (Success)
 */
int main(void)
{
	int index;

	for (index = 0; index < 10; index++)
	{
		printf("%d", index);
	}
	putchar('\n');
	return (0);
}
